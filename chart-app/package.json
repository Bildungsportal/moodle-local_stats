{
  "name": "chart-app",
  "version": "1.0.0",
  "description": "",
  "main": "src/index.ts",
  "scripts": {
    "dev": "npm run prebuild && ts-node src/index.ts",
    "prebuild": "node scripts/generate-version.js",
    "watch": "npm run prebuild && ncc build -w src/index.ts",
    "build": "ncc build src/index.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "prowebat",
  "license": "ISC",
  "dependencies": {
    "canvas": "^3.2.1"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.44.0",
    "chart.js": "^4.5.1",
    "chartjs-node-canvas": "^5.0.0",
    "typescript": "^6.0.2"
  }
}
